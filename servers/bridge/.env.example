# Bridge Server 环境变量

# JWT 密钥（生产环境需改为强密码）
JWT_SECRET=dev-secret-change-in-production

# 可选的 Bridge 密码（不设置时允许免密登录）
# BRIDGE_PASSWORD=your-password-here

# Bridge 监听端口（默认 8080）
BRIDGE_PORT=8080

# OpenCode 服务地址（默认 http://localhost:4096）
OPENCODE_URL=http://localhost:4096
